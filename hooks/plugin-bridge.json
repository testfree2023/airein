{
  "description": "Airein L1 bridge only — SessionStart kernel-ready check. Full iron-law hooks come from airein setup (L2), not this file.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/hooks/plugin-kernel-bridge.js\"",
            "timeout": 5
          }
        ],
        "description": "B→C: warn when ~/.airein kernel is missing (INV-NO-HALVES)"
      }
    ]
  }
}
