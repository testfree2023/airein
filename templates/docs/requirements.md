<!-- COMPATIBILITY STUB — 非权威 requirements 模板正文 -->
<!--
  P005：权威 PRD 模板已分档到：
    templates/docs/requirements/s.md
    templates/docs/requirements/m.md
    templates/docs/requirements/l.md
  生成计划内 requirements.md 前，先调用：
    require('./scripts/lib/requirements-template').resolveRequirementsTemplate(pipeline)
  或阅读 new-plan skill 中的选模步骤。
  禁止把本文件当作「简易需求摘要」或唯一结构权威。
  规模不明且 pipeline 含 requirements 时：默认使用 m.md。
-->

# Requirements template (compat stub)

> **Do not use this file as the PRD structure.**  
> Use `templates/docs/requirements/{s|m|l}.md` via `resolveRequirementsTemplate(pipeline)`.

Default for unknown custom pipelines with a requirements step: **`requirements/m.md`**.
