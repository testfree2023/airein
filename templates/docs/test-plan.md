<!-- COMPATIBILITY STUB — 非权威 test-plan 模板正文 -->
<!--
  权威测试计划模板已分档到：
    templates/docs/test-plan/m.md
    templates/docs/test-plan/l.md
  生成计划内 test-plan.md 前，先调用：
    require('./scripts/lib/test-plan-template').resolveTestPlanTemplate(pipeline)
  或阅读 new-plan skill 中的选模步骤。
  禁止把本文件当作唯一结构权威。
  规模不明且 pipeline 含 test-plan 时：默认使用 m.md。
-->

# Test Plan template (compat stub)

> **Do not use this file as the test-plan structure.**  
> Use `templates/docs/test-plan/{m|l}.md` via `resolveTestPlanTemplate(pipeline)`.

Default for unknown custom pipelines with a test-plan step: **`test-plan/m.md`**.

Plan work ledger remains `templates/docs/tests.md` (per-plan `tests.md`).
